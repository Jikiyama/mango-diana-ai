version: 1
applications:
  - appRoot: .
    frontend:
      phases:
        preBuild:
          commands:
            - nvm install 20
            - nvm use 20
            - npm install -g expo-cli
            - npm install
        build:
          commands:
            - expo export:web
      artifacts:
        baseDirectory: web-build   # <-- This is the fix!
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
